# Strict pinning policy. Any `pnpm add` writes exact versions (no `^`).
# CI uses `pnpm install --frozen-lockfile` to guarantee the lockfile is the source of truth.
save-exact=true
save-prefix=""
engine-strict=false
